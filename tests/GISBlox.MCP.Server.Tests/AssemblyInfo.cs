using Microsoft.VisualStudio.TestTools.UnitTesting;

// Assembly-level attribute to explicitly configure MSTest parallelization
[assembly: Parallelize(Workers = 0, Scope = ExecutionScope.ClassLevel)]
